# reference / 参考项目

此文件夹用于存放参考项目(例如其他 HACS 卡片的源码、模板等)。

- 该目录已被 `.gitignore` 排除,**里面的内容不会被提交到仓库**。
- 请手动 `git clone` 参考项目到此目录,例如:

```bash
git clone https://github.com/custom-cards/boilerplate-card reference/boilerplate-card
```

本目录内的文件仅供本地参考,不影响 `src/` 与 `dist/` 的构建和发布。
